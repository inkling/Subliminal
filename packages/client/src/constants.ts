export enum NotificationCategory {
  COMMENTS = 'comments',
  FEEDBACK = 'feedback',
  ACCESS = 'access',
}

export enum NotificationCategoryLabel {
  comments = 'Comment',
  feedback = 'Feedback',
  access = 'Access Request',
}
