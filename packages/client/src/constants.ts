export enum NotificationCategory {
  COMMENTS = 'comments',
  FEEDBACK = 'feedback',
  ACCESS = 'access',
}

export enum NotificationCategoryLabel {
  comments = 'Comments',
  feedback = 'Feedback',
  access = 'Project Invite',
}
